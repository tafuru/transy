---
phase: 04-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Transy/Settings/SettingsStore.swift
  - Transy/Settings/SettingsView.swift
  - Transy/AppDelegate.swift
  - Transy/TransyApp.swift
  - Transy/Popup/PopupController.swift
  - TransyTests/SettingsStoreTests.swift
  - TransyTests/TargetLanguageSnapshotTests.swift
  - Transy.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - APP-02
must_haves:
  truths:
    - "On first launch, Transy resolves a default target language from the OS preference and persists it."
    - "Once a target language has been stored, later OS language changes do not overwrite that stored choice."
    - "Changing the target language affects the next translation request immediately."
    - "An already-visible popup/request keeps the target language snapshot it started with."
  artifacts:
    - path: "Transy/Settings/SettingsStore.swift"
      provides: "Defaults-backed @Observable target-language source of truth"
      contains: "final class SettingsStore"
    - path: "Transy/AppDelegate.swift"
      provides: "Request-time target-language snapshot wiring for popup requests"
      contains: "settingsStore"
    - path: "Transy/Settings/SettingsView.swift"
      provides: "Compile-safe placeholder Settings API that accepts the shared store before Phase 4 UI work lands"
      contains: "settingsStore"
    - path: "Transy/TransyApp.swift"
      provides: "Settings scene injection of the shared store"
      contains: "SettingsView(settingsStore:"
    - path: "Transy/Popup/PopupController.swift"
      provides: "Popup entry point that accepts a frozen TranslationAvailabilityClient"
      contains: "availabilityClient"
    - path: "TransyTests/SettingsStoreTests.swift"
      provides: "Wave 0 coverage for first-run defaulting and persistence"
      contains: "SettingsStoreTests"
    - path: "TransyTests/TargetLanguageSnapshotTests.swift"
      provides: "Wave 0 coverage for request snapshot behavior"
      contains: "TargetLanguageSnapshotTests"
    - path: "Transy.xcodeproj/project.pbxproj"
      provides: "Generated Xcode project includes the new source and test files"
      contains: "SettingsStore.swift"
  key_links:
    - from: "Transy/TransyApp.swift"
      to: "Transy/Settings/SettingsView.swift"
      via: "Settings scene injection"
      pattern: "SettingsView\\(settingsStore: appDelegate\\.settingsStore\\)"
    - from: "Transy/AppDelegate.swift"
      to: "Transy/Settings/SettingsStore.swift"
      via: "Request-time read of current target language"
      pattern: "settingsStore\\.(targetLanguage|snapshotTargetLanguage)"
    - from: "Transy/AppDelegate.swift"
      to: "Transy/Popup/PopupController.swift"
      via: "Frozen availability client passed on popup show"
      pattern: "show\\(translationCoordinator:.*availabilityClient:"
---

<objective>
Create the shared persisted target-language source of truth and wire translation requests to snapshot that value at trigger time.

Purpose: Satisfy APP-02 without regressing Phase 3 popup behavior by ensuring Settings and runtime translation flow read the same stored value while active popups stay stable.
Output: A `SettingsStore`, a compile-safe injected-store placeholder `SettingsView`, request-time snapshot wiring in `AppDelegate`/`PopupController`, and Wave 0 tests that lock first-run defaulting plus frozen-request behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings/04-CONTEXT.md
@.planning/phases/04-settings/04-RESEARCH.md
@.planning/phases/04-settings/04-VALIDATION.md
@.planning/phases/01-app-shell/01-02-SUMMARY.md
@.planning/phases/03-translation-loop/03-02-SUMMARY.md
@Transy/AppDelegate.swift
@Transy/TransyApp.swift
@Transy/Popup/PopupController.swift
@Transy/Popup/PopupView.swift
@Transy/Translation/TranslationAvailabilityClient.swift

<interfaces>
From `Transy/TransyApp.swift`:
```swift
@main
struct TransyApp: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) private var appDelegate

    var body: some Scene {
        MenuBarExtra("Transy", systemImage: "character.bubble") { ... }
        Settings {
            SettingsView()
        }
    }
}
```

From `Transy/AppDelegate.swift`:
```swift
private let popupController = PopupController()
private let translationCoordinator = TranslationCoordinator()

private func handleTrigger(preSnapshot: [NSPasteboardItem]) {
    _ = translationCoordinator.begin(sourceText: normalizedText)
    popupController.show(translationCoordinator: translationCoordinator) { ... }
}
```

From `Transy/Popup/PopupController.swift`:
```swift
func show(
    translationCoordinator: TranslationCoordinator,
    onDismiss: @escaping () -> Void
)
```

From `Transy/Popup/PopupView.swift`:
```swift
init(
    translationCoordinator: TranslationCoordinator,
    availabilityClient: TranslationAvailabilityClient = TranslationAvailabilityClient()
)
```

From `Transy/Translation/TranslationAvailabilityClient.swift`:
```swift
struct TranslationAvailabilityClient: Sendable {
    let targetLanguage: Locale.Language
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — add failing target-language persistence and snapshot tests first</name>
  <files>Transy/Settings/SettingsStore.swift, TransyTests/SettingsStoreTests.swift, TransyTests/TargetLanguageSnapshotTests.swift, Transy.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: First run resolves the default target language from an injected OS-preferred-language seam and persists that minimalIdentifier into `UserDefaults`.
    - Test 2: Once a stored target exists, later changes in the injected OS-preferred-language seam do not overwrite it.
    - Test 3: A request snapshot captured before `updateTargetLanguage(_:)` keeps the old target, while a new snapshot taken after the update sees the new target.
  </behavior>
  <action>Create `SettingsStore` as an `@MainActor @Observable` contract with only the minimum compile-ready scaffold needed for tests. Inject `UserDefaults` and a preferred-language resolver so tests can run without mutating global defaults or the real OS language list. Add the two Wave 0 Swift Testing files named in `04-VALIDATION.md`. If Xcode needs regeneration to discover the new test files, run `xcodegen generate` rather than editing `Transy.xcodeproj` directly. Do not add UI code, provider settings, searchable lists, or any live popup mutation here.</action>
  <verify>
    <automated>bash -lc 'set +e; xcodebuild test -scheme Transy -destination '"'"'platform=macOS'"'"' -only-testing:TransyTests/SettingsStoreTests -only-testing:TransyTests/TargetLanguageSnapshotTests; status=$?; test $status -ne 0'</automated>
  </verify>
  <done>`SettingsStoreTests` and `TargetLanguageSnapshotTests` exist, compile, and fail for the missing persistence/snapshot behavior rather than for unrelated scaffolding gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared store, scene injection, and request-time snapshot wiring</name>
  <files>Transy/Settings/SettingsStore.swift, Transy/Settings/SettingsView.swift, Transy/AppDelegate.swift, Transy/TransyApp.swift, Transy/Popup/PopupController.swift, Transy.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: The stored target language round-trips through `UserDefaults` using `Locale.Language.minimalIdentifier`.
    - Test 2: The initial value comes from the OS preference only when no stored value exists, and stays pinned once stored.
    - Test 3: `AppDelegate` freezes the target language for each popup request so the next request sees changes immediately while the current popup stays unchanged.
  </behavior>
  <action>Finalize `SettingsStore` as the single persisted source of truth owned by `AppDelegate`. Persist only the language `minimalIdentifier`; reconstruct `Locale.Language` from that identifier; auto-save immediately on update; and expose a tiny request-snapshot seam so runtime code can freeze the target before creating the request-scoped `TranslationAvailabilityClient`. Update `TransyApp` so the existing native `Settings` scene receives `appDelegate.settingsStore`, and make the current placeholder `SettingsView` compile with that injected store even before the real Phase 4 UI lands. Update `PopupController.show(...)` to accept an injected `TranslationAvailabilityClient` and pass that frozen client into `PopupView` instead of relying on the old generic-English default. Keep the Phase 1 `Settings` scene / activation-policy pattern intact, and do not bind the popup live to `SettingsStore`.</action>
  <verify>
    <automated>xcodebuild test -scheme Transy -destination 'platform=macOS' -only-testing:TransyTests/SettingsStoreTests -only-testing:TransyTests/TargetLanguageSnapshotTests</automated>
  </verify>
  <done>A single store instance is shared between app runtime and Settings, stored values survive relaunch, and popup requests use a frozen target-language client captured at trigger time.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -scheme Transy -destination 'platform=macOS' -only-testing:TransyTests` after each task commit per `04-VALIDATION.md`. Before closing the plan, confirm no code path still relies on the generic-English default for newly created popup requests.
</verification>

<success_criteria>
- `SettingsStore` owns persistent target-language state with no duplicate runtime source of truth.
- `TransyApp` injects the shared store into the native Settings scene, and the placeholder `SettingsView` still compiles with that injected-store API until Plan 02 replaces the UI.
- `AppDelegate` snapshots the target language before popup creation, and `PopupController` passes that frozen client into `PopupView`.
- `SettingsStoreTests` and `TargetLanguageSnapshotTests` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings/04-01-SUMMARY.md`
</output>
