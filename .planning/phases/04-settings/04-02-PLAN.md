---
phase: 04-settings
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Transy/AppDelegate.swift
  - Transy/Popup/PopupController.swift
  - Transy/Popup/PopupView.swift
  - Transy/Settings/SettingsStore.swift
  - Transy/Settings/SettingsView.swift
  - Transy/Settings/SupportedLanguageOption.swift
  - Transy/Settings/TranslationModelGuidance.swift
  - Transy/Translation/TranslationAvailabilityClient.swift
  - TransyTests/TranslationModelGuidanceTests.swift
  - Transy.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - APP-02
  - APP-03
must_haves:
  truths:
    - "Opening Settings shows one compact native pane with a broad target-language picker using natural-language names only."
    - "Changing the picker auto-saves immediately and the stored choice is still selected after relaunch."
    - "Settings shows no extra guidance before any real missing-model-relevant runtime state has been recorded."
    - "After a real missing-model event, Settings shows short generic download guidance when the source/target pair is not known with confidence."
    - "The guidance seam may upgrade to pair-specific copy only when trusted source/target context is explicitly available; Phase 4 approval does not depend on manufacturing that state manually."
  artifacts:
    - path: "Transy/AppDelegate.swift"
      provides: "Settings store passed into popup flow so missing-model relevance can be recorded without mutating active requests"
      contains: "settingsStore"
    - path: "Transy/Popup/PopupController.swift"
      provides: "Popup entry point that forwards the settings store / guidance sink into PopupView"
      contains: "settingsStore"
    - path: "Transy/Popup/PopupView.swift"
      provides: "Runtime-to-settings missing-model relevance recording with no live popup mutation"
      contains: "recordMissingModel"
    - path: "Transy/Settings/SettingsStore.swift"
      provides: "Shared guidance-relevance state for none/generic/pair-specific settings guidance"
      contains: "missingModel"
    - path: "Transy/Settings/SettingsView.swift"
      provides: "Real compact settings pane with picker and conditional guidance"
      contains: "struct SettingsView"
    - path: "Transy/Settings/SupportedLanguageOption.swift"
      provides: "Localized supported-language loader derived from LanguageAvailability.supportedLanguages"
      contains: "SupportedLanguageOption"
    - path: "Transy/Settings/TranslationModelGuidance.swift"
      provides: "Generic-vs-pair-specific guidance seam and System Settings action"
      contains: "TranslationModelGuidance"
    - path: "Transy/Translation/TranslationAvailabilityClient.swift"
      provides: "Preflight outcome that distinguishes missing-model relevance from unsupported/installed states without string-matching"
      contains: "PreflightResult"
    - path: "TransyTests/TranslationModelGuidanceTests.swift"
      provides: "Wave 0 coverage for settings guidance logic"
      contains: "TranslationModelGuidanceTests"
    - path: "Transy.xcodeproj/project.pbxproj"
      provides: "Generated Xcode project includes the new settings source and test files"
      contains: "TranslationModelGuidance.swift"
  key_links:
    - from: "Transy/Settings/SettingsView.swift"
      to: "Transy/Settings/SettingsStore.swift"
      via: "Picker binding / immediate auto-save"
      pattern: "settingsStore\\.(targetLanguage|updateTargetLanguage)"
    - from: "Transy/Popup/PopupView.swift"
      to: "Transy/Settings/SettingsStore.swift"
      via: "Record missing-model relevance from real runtime outcomes"
      pattern: "settingsStore\\.recordMissingModel"
    - from: "Transy/Settings/SupportedLanguageOption.swift"
      to: "Translation.LanguageAvailability"
      via: "Supported target-language load"
      pattern: "supportedLanguages"
    - from: "Transy/Settings/TranslationModelGuidance.swift"
      to: "Transy/Settings/SettingsStore.swift"
      via: "Derive none vs generic vs pair-specific guidance from stored relevance context"
      pattern: "missingModel"
    - from: "Transy/Translation/TranslationAvailabilityClient.swift"
      to: "Transy/Popup/PopupView.swift"
      via: "Precise preflight outcome distinguishes missing-model relevance from unrelated failures"
      pattern: "PreflightResult\\.(missingModel|ready|unsupported|couldNotDetect|failed)"
    - from: "Transy/Settings/TranslationModelGuidance.swift"
      to: "Translation.LanguageAvailability"
      via: "Optional known-pair status check when trusted source context exists"
      pattern: "status\\(from:"
    - from: "Transy/Settings/TranslationModelGuidance.swift"
      to: "System Settings"
      via: "Single guidance action"
      pattern: "NSWorkspace\\.shared\\.open"
---

<objective>
Replace the placeholder Settings screen with the real compact picker-driven UI and quiet model-download guidance.

Purpose: Finish APP-02 and APP-03 with a native-feeling settings experience that stays small by default, expands only when guidance matters, and avoids brittle in-app model-management behavior.
Output: A production Settings pane, supported-language loading, runtime-backed guidance-relevance logic, and a manual verification checkpoint for LSUIElement-safe settings behavior and the System Settings action.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings/04-CONTEXT.md
@.planning/phases/04-settings/04-RESEARCH.md
@.planning/phases/04-settings/04-VALIDATION.md
@.planning/phases/04-settings/04-01-PLAN.md
@.planning/phases/01-app-shell/01-02-SUMMARY.md
@.planning/phases/03-translation-loop/03-02-SUMMARY.md
@Transy/Settings/SettingsView.swift
@Transy/MenuBar/MenuBarView.swift
@Transy/Translation/TranslationAvailabilityClient.swift

<interfaces>
From `Transy/MenuBar/MenuBarView.swift`:
```swift
Button("Settings…") {
    NSApp.activate()
    openSettings()
}
```

From `Transy/Translation/TranslationAvailabilityClient.swift`:
```swift
struct TranslationAvailabilityClient: Sendable {
    let targetLanguage: Locale.Language

    func preflight(for sourceText: String) async throws -> PreflightResult
}
```

Expected from `04-01`:
```swift
@MainActor
@Observable
final class SettingsStore {
    private(set) var targetLanguage: Locale.Language
    func updateTargetLanguage(_ language: Locale.Language)
    func snapshotTargetLanguage() -> Locale.Language
}
```

Research constraints to preserve:
- Use `LanguageAvailability.supportedLanguages` for picker population.
- Build display labels from `Locale.current.localizedString(forIdentifier: language.minimalIdentifier)`.
- Keep generic guidance as the truthful fallback whenever a trusted source language is not known.
- Do not depend on an unverified Translation Languages deep link as the only guidance path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — lock guidance-state behavior with focused tests</name>
  <files>Transy/Settings/SettingsStore.swift, Transy/Settings/TranslationModelGuidance.swift, TransyTests/TranslationModelGuidanceTests.swift, Transy.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: Before any real missing-model-relevant runtime state exists, Settings guidance is `.none`.
    - Test 2: After a recorded missing-model event with unknown pair/source certainty, guidance becomes generic and never guesses pair-specific copy.
    - Test 3: When trusted known source language context is explicitly available and `LanguageAvailability.status(from:to:) == .supported`, the seam can derive pair-specific guidance; installed/unsupported paths still return no guidance.
  </behavior>
  <action>Create the small guidance-state seam called for in `04-VALIDATION.md`, and extend `SettingsStore` with the minimum shared guidance-relevance context needed to represent three truthful states: no guidance, generic guidance after a real missing-model event with unknown pair certainty, and optional pair-specific guidance when trusted source context is explicitly available. Keep the seam UI-agnostic with injected status/open handlers so unit tests do not depend on live Apple model assets or actual System Settings UI. Write the Wave 0 tests first, and never infer or guess a source language from ambiguous state.</action>
  <verify>
    <automated>bash -lc 'set +e; xcodebuild test -scheme Transy -destination '"'"'platform=macOS'"'"' -only-testing:TransyTests/TranslationModelGuidanceTests; status=$?; test $status -ne 0'</automated>
  </verify>
  <done>`TranslationModelGuidanceTests` exists, compiles, and fails only because the real guidance behavior is not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the compact Settings UI, supported-language picker, and guidance action</name>
  <files>Transy/AppDelegate.swift, Transy/Popup/PopupController.swift, Transy/Popup/PopupView.swift, Transy/Settings/SettingsStore.swift, Transy/Settings/SettingsView.swift, Transy/Settings/SupportedLanguageOption.swift, Transy/Settings/TranslationModelGuidance.swift, Transy/Translation/TranslationAvailabilityClient.swift, Transy.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: Settings shows no guidance before any real missing-model-relevant state exists.
    - Test 2: After a real missing-model outcome is recorded from runtime, Settings shows truthful generic guidance until trusted source context exists.
    - Test 3: If trusted known-pair context is explicitly available, the same guidance seam can render pair-specific copy without changing the one-action UX.
    - Test 4: The Settings pane loads supported target languages from Apple’s API, shows natural-language labels only, and binds selection changes directly to `SettingsStore`.
  </behavior>
  <action>Replace the placeholder `SettingsView` with one compact native-feeling pane. Load picker rows from `LanguageAvailability.supportedLanguages`, map each row’s title from `language.minimalIdentifier`, and sort naturally. Bind selection directly to `SettingsStore` so changes auto-save with no Apply button. Add an explicit runtime→settings guidance-state path: refine `TranslationAvailabilityClient.PreflightResult` so the popup/runtime can distinguish a real missing-model outcome from unsupported/unknown/other failures without string-matching, then wire `AppDelegate` / `PopupController` / `PopupView` so a real missing-model outcome records relevance into `SettingsStore` without mutating the active popup/request. The Settings pane must then show no guidance before any relevant state exists, generic guidance after a relevant missing-model event when pair/source certainty is unavailable, and only upgrade to pair-specific copy when trusted source context is explicitly available. Keep copy minimal and controls-first. Implement one clear System Settings action using `NSWorkspace.shared.open` with a conservative destination/copy path that still works if a translation-anchor deep link is unavailable. Do not add searchable pickers, provider switching, popup redesign, or in-app model downloads.</action>
  <verify>
    <automated>xcodebuild test -scheme Transy -destination 'platform=macOS' -only-testing:TransyTests/TranslationModelGuidanceTests</automated>
  </verify>
  <done>The placeholder Settings screen is replaced by the real pane, supported target languages load from Apple’s API, runtime missing-model relevance reaches Settings truthfully, and guidance remains short, conditional, and free of guessed pair details.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of settings-window behavior and guidance action</name>
  <what-built>Native Settings scene integration, persisted target-language picker, compact/expanded guidance states, and the single System Settings guidance action.</what-built>
  <action>Run the full Transy unit suite first, then perform the live macOS checks below against the built app. Treat any failure here as blocking feedback for the plan rather than as a follow-up idea; the Settings window, persistence, request snapshot behavior, and guidance action all need explicit human confirmation on-device.</action>
  <how-to-verify>
    1. Run `xcodebuild test -scheme Transy -destination 'platform=macOS' -only-testing:TransyTests` and confirm the suite stays green before launching the app.
    2. Launch Transy, open `Settings…` from the menu bar, and confirm one native Settings window appears above the current app without Dock-icon regression or stacked duplicate windows.
    3. Confirm the default settings pane is compact and controls-first, with the target-language picker shown using natural-language names only.
    4. Choose a non-default target language, close Settings, quit/relaunch Transy, reopen Settings, and confirm the same language is still selected.
    5. Trigger one translation, change the target language while that popup is still visible, and confirm the visible popup does not mutate; then trigger again and confirm the next request uses the new target.
    6. On a fresh/source-unknown state, open Settings and confirm no extra model guidance is shown before any missing-model-relevant runtime state exists.
    7. Trigger a translation path that produces the Phase 3 missing-model outcome, reopen Settings, and confirm the pane now shows short generic guidance with one clear System Settings action rather than guessed pair-specific copy.
    8. Click the guidance action and confirm it opens System Settings to the relevant Language & Region / Translation Languages area, or at minimum lands in the correct Settings area with copy that makes the next step obvious and without noisy automation prompts.
    9. Pair-specific guidance is covered by unit tests and should only be manually checked if a trustworthy known-pair source context is naturally available during implementation; Phase 4 approval does not depend on manufacturing that state on demand.
  </how-to-verify>
  <verify>
    <automated>xcodebuild test -scheme Transy -destination 'platform=macOS' -only-testing:TransyTests</automated>
  </verify>
  <done>Human verification explicitly approves window surfacing, persistence across relaunch, no mid-flight popup mutation, compact/expanded guidance behavior, and the single System Settings action.</done>
  <resume-signal>Type "approved" if all checks pass, or describe the exact failing step(s) and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
Follow `04-VALIDATION.md`: run `xcodebuild test -scheme Transy -destination 'platform=macOS' -only-testing:TransyTests` after each task commit, then run the full Phase 4 manual checklist before closing the plan. Manual validation must explicitly cover settings-window surfacing, compact-vs-expanded behavior, the no-mid-flight-mutation rule, and the System Settings guidance action.
</verification>

<success_criteria>
- Settings opens as the existing native single-instance Settings scene with no Dock/app-switcher regression.
- The target-language picker is broad, native, and populated from Apple’s supported-language list using natural-language labels only.
- Target-language changes auto-save immediately and remain selected after relaunch.
- Guidance is absent when irrelevant, generic after a real missing-model event with unknown pair certainty, and only upgrades to pair-specific copy when trusted known-pair context exists.
- The guidance action gives the user one clear path into System Settings without relying solely on a brittle deep link.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings/04-02-SUMMARY.md`
</output>
